interface applyProps {
  params: { jobId: string };
}

const ApplyJob = ({ params }: applyProps) => {
  return (
    <main>
      <div>ApplyJob</div>
    </main>
  );
};

export default ApplyJob;
